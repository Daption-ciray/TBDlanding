# Railway deployment config – Laravel
# https://docs.railway.app/reference/config-as-code

[build]
builder = "nixpacks"

[# Let Nixpacks detect the correct start command (php artisan serve ...)
# and working directory automatically. We only keep restart policy here.]
[deploy]
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
