# Railway deployment config – Laravel
# https://docs.railway.app/reference/config-as-code

[build]
builder = "nixpacks"

[deploy]
startCommand = "cd /workspace && php artisan serve --host=0.0.0.0 --port=$PORT"
releaseCommand = "cd /workspace && php artisan migrate --force"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
