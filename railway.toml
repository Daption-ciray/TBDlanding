# Railway deployment configuration
[build]
builder = "nixpacks"

[deploy]
startCommand = "chmod -R 777 storage bootstrap/cache && php artisan migrate:fresh --force && php artisan serve --host=0.0.0.0 --port=${PORT:-8080}"
healthCheckPath = "/up"
healthCheckTimeout = 120
